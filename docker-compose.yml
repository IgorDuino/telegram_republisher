services:
  db:
    user: 1000:1000
    image: postgres
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - 54321:5432
    volumes:
      - ./data:/var/lib/postgresql/data